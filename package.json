{
  "name": "pet-helper-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "install-all": "npm install --prefix backend"
  }
}
